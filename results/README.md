# Results directory
This directory stores evaluation results from each benchmark run.
Each run is saved as a separate file (results_v1.md, results_v2.md, etc.)
to preserve history — never overwrite a previous run.
