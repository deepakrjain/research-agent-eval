# Research Agent with Self-Graded Answer Sheet

An AI agent that autonomously searches the web, reads multiple sources, decides when it has enough information, and writes cited answers — then evaluates its own quality using an independent LLM judge with a human-validated benchmark.
